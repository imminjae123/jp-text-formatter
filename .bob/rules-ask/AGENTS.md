# Project Documentation Rules (Non-Obvious Only)

- The spec is in `jp-text-formatter.md` (Japanese). The `README.md` is just a title line — ignore it.
- There is no source code yet; `jp-text-formatter.md` is the sole authoritative reference.
- The project was originally spec'd as a single-file app; it was intentionally refactored to a 3-file split (`index.html`, `style.css`, `script.js`) for maintainability. Earlier references to "1ファイル完結型" are outdated.
