# 開発依頼：日本語テキスト「半角・全角」一括変換＆整形ツール（ファイル分割構成）

日本のオフィス業務で発生する「テキストの表記揺れ」を一瞬で解消するための、ローカル（ブラウザ）で動作する静的Webアプリを開発してください。

## 技術スタック
- HTML5, CSS3, JavaScript (ES6+)
- 外部サーバーへの通信は一切行わず、すべての処理をブラウザ上で完結させること（個人情報・機密情報の保護のため）。
- CSSフレームワーク（Tailwind CSS など）をCDN経由で読み込み、モダンでクリーンなデザインにしてください。

## ファイル構成（3ファイル分割）

メンテナンス性を考慮し、以下の3ファイルに分割してください。

```
index.html   # 骨組み（HTML構造）と Tailwind CSS CDN の読み込み
style.css    # カスタムデザイン・アニメーション（index.html から <link> で読み込む）
script.js    # 全角・半角の双方向変換ロジック、イベント処理（index.html から <script src> で読み込む）
```

## 画面レイアウト（UI要件）
- **レスポンシブデザイン：** PCとスマートフォンの両方に対応。
- **入力エリアと出力エリア：** 左右、または上下に並べて配置（入力エリアにテキストを入れると、右側または下部に結果が出る）。
- **設定パネル（チェックボックス）：**
  - 英数字を半角に、カタカナを全角に一括変換（デフォルトON）
  - 全角スペースを半角スペースに変換（デフォルトON）
  - 行頭・行末の不要なスペースを削除（トリム）（デフォルトON）
  - 連続する改行を1つにまとめる（デフォルトOFF）
- **操作ボタン：**
  - 「変換する」ボタン（またはリアルタイム自動変換）
  - 「結果をクリップボードにコピー」ボタン
  - 「入力をクリア」ボタン
- **情報表示：**
  - 入力文字数、出力文字数をそれぞれリアルタイムでカウントして表示。

## 機能要件（JavaScript — script.js に実装）
- 正規表現を用いて、選択されたチェックボックスのルールに従いテキストを高速処理すること。
- 全角英数字 ⇄ 半角英数字、半角カタカナ ⇄ 全角カタカナの変換テーブルを適切に実装すること。
- コピーボタンが押された際は、「コピーしました！」というトースト通知、もしくはボタンのテキスト変化でフィードバックを行うこと。

美しく、社内の誰もが直感的に使えるUIのツールに仕上げてください。
